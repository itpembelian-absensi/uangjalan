import sys; sys.path.append('backend'); from app.db import SessionLocal; from app.models import Sale, SaleDetail; db = SessionLocal(); s = db.query(Sale).filter_by(sale_no='UJ26060020').first(); print('Sale:', s.id if s else None); [print('Detail:', d.customer_id, d.vehicle_type_id, d.amount) for d in db.query(SaleDetail).filter_by(sale_id=s.id).all()] if s else print('No Sale')  
