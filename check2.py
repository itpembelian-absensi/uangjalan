import sys; sys.path.append('backend'); from app.db import SessionLocal; from app.models import CustomerVehicleTariff; db = SessionLocal(); t = db.query(CustomerVehicleTariff).filter_by(customer_id=5352, vehicle_type_id=4).first(); print('Tariff:', t.bbm if t else None, t.tol if t else None, t.uang_mel if t else None, t.parkir if t else None, t.lain_lain if t else None, t.uang_jalan if t else None)  
