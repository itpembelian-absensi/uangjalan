import sys; sys.path.append('backend'); from app.db import SessionLocal; from app.models import Sale, SaleDetail; db = SessionLocal(); sale = db.query(Sale).filter(Sale.sale_no=='UJ26060020').first(); print('Sale:', sale.id if sale else None, sale.extra_uang_jalan if sale else None); [print('Detail:', d.customer_id, d.vehicle_type_id, d.amount) for d in db.query(SaleDetail).filter(SaleDetail.sale_id == sale.id).all()] if sale else print('No Sale')  
